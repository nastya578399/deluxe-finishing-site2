// (function () {
//   var square = document.querySelector('.header-logo');

//   var observer = new IntersectionObserver(entries => {
//     entries.forEach(entry => {
//       if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
//         return;
//       }

//       if (entry.isIntersecting) {
//         entry.target.classList.add('_active');
//       }
//     });
//   });

//   observer.observe(square);
// })();

(function () {
    var square = document.querySelector('.main-text-one');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.main-text-two');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.help-text');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.section-title');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.text-three');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();
  (function () {
    var square = document.querySelector('.text-three');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();
  (function () {
    var square = document.querySelector('.text-three');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.text-three4');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.text-three5');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.text-three5');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.text-three6');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.image-three');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();


  (function () {
    var square = document.querySelector('.image-table');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.image-phone1');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.image-phone2');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.image-three5');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.image-three6');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();


  (function () {
    var square = document.querySelector('.image-table5');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.image-phone1-5');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.image-phone2-5');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.image-table6');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.image-phone1-6');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.image-phone2-6');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();


  // section3

  (function () {
    var square = document.querySelector('.section-title2');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();


  (function () {
    var square = document.querySelector('.section-title4');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.section-title5');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.section-title6');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  // 4

  (function () {
    var square = document.querySelector('.image-table4');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.image-phone1-4');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.image-phone2-4');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();


  (function () {
    var square = document.querySelector('.section9-text-one');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.section3-img');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.section8-flex-img-46');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('section8-active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.section8-flex-img-48');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('section8-active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.section8-flex-img-47');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('section8-active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.section8-flex-img-38');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('section8-active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.section7-img-one');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('section7-img-active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.section7-img-two');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('section7-img-active');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.text-tablet');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('active-text');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.text-phone1');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('active-text');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.text-phone2');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('active-text');
        }
      });
    });
  
    observer.observe(square);
  })();

  (function () {
    var square = document.querySelector('.square9-main-blok2');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('_active');
        }
      });
    });
  
    observer.observe(square);
  })();




